# Brief

```
/browser-extension-builder "Chrome extension that highlights tracking scripts on every page and shows the data they exfiltrate"
```

## Why this example

Shows Tanker outside the SaaS-MVP path. Browser-extension-builder is a vertical skill with a different output shape (manifest.json, content scripts, message passing).

## Expected runtime

- ~15 minutes.
- Cost: ~$1.50.
- Output: ready-to-load unpacked extension folder.
